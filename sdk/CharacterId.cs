namespace Grandia.Sdk;

/// <summary>
/// Playable ids at MapObj+0x10C (stride 0x80). Same numbers
/// <see cref="GameParty"/> / <see cref="OnBattleLoadAttribute"/> use.
/// </summary>
public enum CharacterId
{
    Justin = 1,
    Feena = 2,
    Sue = 3,
    Gadwin = 4,
    Rapp = 5,
    Milda = 6,
    Guido = 7,
    Liete = 8,
}
